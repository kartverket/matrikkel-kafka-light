apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: matrikkel-kafka-light
  namespace: matrikkel-main
spec:
  image: "{{ image }}"
  port: 8081
  ingresses:
    - matrikkel-kafka-light.atkv3-prod.kartverket-intern.cloud
  replicas: 1

  resources:
    requests:
      cpu: 50m
      memory: 256Mi
    limits:
      memory: 512Mi

  liveness:
    port: 8081
    path: /internal/isAlive
    initialDelay: 10      # default: 0
    failureThreshold: 6   # default: 3
  readiness:
    port: 8081
    path: /internal/isReady
    initialDelay: 10      # default: 0
    failureThreshold: 6   # default: 3
  prometheus:
    port: 8081
    path: /internal/metrics
    allowAllMetrics: true

  azure:
    application:
      enabled: true

  accessPolicy:
    inbound:
      rules:
        - application: matrikkel-ekstern-data-ingestor
        - application: matrikkel-serg-sync

  databases:
    - name: matrikkel-kafka-light
      envName: DB_URL

  env:
    - name: VERSION
      value: "{{ version }}"
    - name: DB_USER_USERNAME
      value: "kafka_light_user"
    - name: DB_USER_PASSWORD
      gsmSecretName: "prod-matrikkel-kafka-light-db-user-password"
    - name: DB_ADMIN_USERNAME
      value: "kafka_light_admin"
    - name: DB_ADMIN_PASSWORD
      gsmSecretName: "prod-matrikkel-kafka-light-db-admin-password"
    - name: AZURE_AD_SERVICE_PRINCIPAL_MATRIKKEL_EKSTERN_DATA_INGESTOR
      value: "d21c5148-833e-482f-b798-a83bbc5198ad"
    - name: AZURE_AD_SERVICE_PRINCIPAL_MATRIKKEL_SERG_SYNC
      value: "8a595c51-9fdd-470b-b0dc-9aa8a1ebba75"
