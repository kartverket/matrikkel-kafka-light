# matrikkel-kafka-light

This project is under development and is meant to serve as a distributed streaming system: with a broker app, and packages for event producers and consumers

## Gradle
This project uses gradle

## Local compose
This project includes a docker compose that starts the broker app and a local postgres database.

    `docker compose up --build`
